{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "Sorting Order",
    "description": "The sorting order used to sort elements relative to each other.",
    "type": "object",
    "properties": {
        "after": {
            "description": "The element that this element should come after.",
            "type": "string",
            "pattern": "\\w+:\\w+"
        },
        "order": {
            "description": "The numerical sort order of this element.",
            "type": "number"
        }
    },
    "oneOf": [
        {
            "required": [
                "after"
            ]
        },
        {
            "required": [
                "order"
            ]
        }
    ]
}